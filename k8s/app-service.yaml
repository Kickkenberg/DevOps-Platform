apiVersion: v1 
kind: Service 

metadata:
  name: demo-app-service
  namespace: devops-platform

spec:
  selector:
    app: demo-app 

  ports:
    - protocol: TCP 
      port: 80
      targetPort: 5000
    
  type: ClusterIP